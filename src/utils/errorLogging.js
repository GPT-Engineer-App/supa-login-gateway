export const logError = (message, error) => {
  console.error(message, error);
  // TODO: Implement more robust error logging here, such as sending to a logging service
};
export const logError = (message, error) => {
  console.error(message, error);
  // TODO: Implement more robust error logging here, such as sending to a logging service
};
